import { Constructor } from '@loopback/context';
export interface AuthenticationMetadata {
    strategy: string;
    options?: Object;
}
/**
 * decorator to add authentication metadata to controller methods
 * @param strategyName
 * @param options
 */
export declare function authenticate(strategyName: string, options?: Object): (controllerClass: Object, methodName: string) => void;
/**
 * function to get stored authentication metadata in a controller method
 * @param controllerObj
 * @param methodName
 */
export declare function getAuthenticateMetadata(controllerClass: Constructor<{}>, methodName: string): AuthenticationMetadata | undefined;
